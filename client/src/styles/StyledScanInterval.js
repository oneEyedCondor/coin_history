import styled from 'styled-components';

export default styled.div`
    .scan-interval {
        margin-bottom: 20px;
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: space-between;

        select {
            outline: none;
            cursor: pointer;
        }
    }
`;
